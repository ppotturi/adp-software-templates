apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: example-domains
  description: A collection of all Backstage example domains
spec:
  targets:
    - ./domains/grants-domain.yaml
    - ./domains/payments-domain.yaml