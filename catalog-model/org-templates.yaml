apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: org-templates
  description: A collection of all Backstage org templates
spec:
  targets:
    - ../templates/ffc-template-node/template.yaml
